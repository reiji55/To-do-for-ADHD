# 接続先を差し替えたいときだけ、このファイルを .env という名前でコピーして使う。
# 何も置かなければ src/App.jsx に書いてあるフォールバック（現在のプロジェクト）に繋がる。
#
# anon key は公開前提の鍵。RLS が有効なので、これだけでは他人のデータには触れない。
# service_role キーは絶対にここへ書かないこと（RLS を貫通する）。

VITE_SUPABASE_URL=https://xinbecreamkzltdchway.supabase.co
VITE_SUPABASE_ANON_KEY=ここに anon key
